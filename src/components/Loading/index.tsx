import { HeadingPrimary } from './styles';

import { Wrapper } from './styles';

const Loading = () => {
  return (
    <Wrapper>
      <HeadingPrimary>Please wait...</HeadingPrimary>
    </Wrapper>
  );
};

export default Loading;
