import styled from 'styled-components';
import Theme from '../Theme';

export const Container = styled.div`
  width: 100%;
  padding: 1.5rem 2rem;
  background-color: ${Theme.colorPalette.error};
`;

export const HeadingTertiary = styled.h2`
  font-size: 1.8rem;
`;
